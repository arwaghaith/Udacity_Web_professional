   <!-- Navigation starts as empty UL that will be filled with JS 
         sections are added dynamically by using javascript 
         "* create four-section dynamically by javascript instead of HTML
 * create them links
 * ability to observe sections
 */"
         going to the top of the page using to_top that appears after 700px down ------------>
